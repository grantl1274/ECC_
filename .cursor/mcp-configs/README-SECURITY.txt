This mcp-servers.json file is a TEMPLATE only (YOUR_*_HERE placeholders).
Cursor loads .cursor/mcp.json — keep that file empty or explicitly configured.
Do not copy placeholder values into live MCP configs with real secrets.
